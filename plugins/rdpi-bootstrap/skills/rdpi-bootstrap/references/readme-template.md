# RDPI Workflow — {{PROJECT_NAME}}

Generated by `rdpi-bootstrap` on {{DATE}}.

## What is RDPI?

RDPI splits AI-assisted development into 4 phases, each running in a **fresh context** with structured artifact handoff. This prevents context degradation and ensures quality on medium-to-large tasks.

Based on [Dex Horthy's Context Engineering (QRSPI)](https://www.youtube.com/watch?v=example) and Dmitry Bereznitsky's "Process over Prompts."

## Phase Commands

| Phase | Command |
|-------|---------|
| Research | `/rdpi-research <task description or task ID>` |
| Design | `/rdpi-design US1234` |
| Plan | `/rdpi-plan US1234` |
| Implement | `/rdpi-implement US1234` |

> Clear context between each phase — open a fresh session before running the next command.

RDPI maps to QRSPI (Dex Horthy's methodology): **Q**uestions → **R**esearch → **D**esign → **S**tructure → **P**lan → **I**mplement. Each phase runs one or more of these stages internally, with sub-agents for isolation.

| Phase | Sub-phase | Who | Output | User action |
|-------|-----------|-----|--------|-------------|
| **Research** | Questions | Main ↔ user | Interview notes | Answer questions |
| | Blind research | Sub-agent | `research.md` | — |
| | Spec writing | Main agent | `spec.md` | Review (optional) |
| | Complexity check | Main agent | Next step rec. | Choose next phase |
| **Design** | Interactive design | Main ↔ user | `design.md` | Shape the design |
| | Structure/Outline | Sub-agent | `structure-outline.md` | **Review (mandatory)** |
| **Plan** | Plan generation | Sub-agent | `plan.md` | — |
| | Spec consistency | Sub-agent | `spec-consistency.md` | — |
| | Summary | Main agent | `summary.md` | Read summary |
| **Implement** | Branch setup | Orchestrator | Git branch | — |
| | Slice execution | Sub-agents | Code | **Test first slice** |
| | Draft PR | Sub-agent | PR link | **Code review** |
| | Deploy/verify | Sub-agents | Logs, screenshots | Verify (opt-in) |

*Design and Plan are optional — Research recommends which phases to skip based on task complexity.*

## Quick Start

```
/rdpi-research <describe your task or paste a ticket>
```

Then follow the recommendations at the end of each phase. The typical flow:

```
/rdpi-research  →  /rdpi-design  →  /rdpi-plan  →  /rdpi-implement
     ↓                  ↓                ↓                ↓
  spec.md          design.md         plan.md          code + PR
```

**Important:** Clear your context (start a new conversation) between each phase. The artifacts on disk carry all the state — the context window is disposable.

## Complexity-Based Shortcuts

Research recommends the next phase based on task complexity:

- **Complex** → Research → Design → Plan → Implement (full pipeline)
- **Medium** → Research → Plan → Implement (skip Design)
- **Simple** → Research → Implement (skip Design + Plan)

You always decide — the agent just recommends.

## Artifact Structure

Each task creates a folder:
```
./rdpi/{{ARTIFACT_FOLDER_FORMAT_EXAMPLE}}/
├── 01-research/
│   ├── questions.md       — Research questions
│   ├── research.md        — Blind research findings
│   └── spec.md            — Requirements spec
├── 02-design/
│   ├── design.md          — Architecture decisions
│   ├── structure-outline.md — Vertical slices breakdown
│   └── c4-diagrams.md     — Architecture diagrams
├── 03-plan/
│   ├── plan.md            — Execution plan (for the agent)
│   ├── spec-consistency.md — Plan vs Spec check
│   └── summary.md         — Brief summary (for you)
└── 04-implement/
    ├── implementation-log.md — Progress log
    └── pr-summary.md        — PR link and description
```

## Configuration

Your RDPI preferences are saved in `./rdpi/RDPI_SKILLS_SPEC.md`. To update them, run `/rdpi-bootstrap` again.

## Skills Location

{{#if MARKETPLACE}}
Skills are registered as a global marketplace at `{{MARKETPLACE_PATH}}`. They are available across projects via `~/.claude/settings.json`.
{{else}}
Skills live in `.claude/skills/` and are auto-discovered by Claude Code — no extra configuration needed.
{{/if}}

## Tips

- **For small fixes**, skip RDPI entirely — just prompt directly. RDPI pays off on tasks that take more than ~30 minutes of implementation.
- **Read the code.** RDPI helps structure the work, but you should still review the code in the PR.
- **Restart over repair.** If a phase goes sideways, restart it with fresh context rather than trying to fix a bad trajectory.
